{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "make default_district.xtbl",
      "windows": {
        "command": "python",
        "args": ["${workspaceFolder}\\main.py"],
        "options": {
          "cwd": "${workspaceFolder}"
        }
      }
    }
  ]
}
