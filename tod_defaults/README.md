# tod_defaults
It stores the default values that will be used for each keyframe if no value is found in the tod_override file.

### sr3_base_district
Has default values ​​from te

### sr4_default_district
Values ​​from the first keyframe in the sr4 default_district.xtbl
