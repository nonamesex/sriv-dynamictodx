# district_unpack

This script allows you to split `default_district.xtbl` into tod_overrides files by keyframes.

Place `default_district.xtbl` here and run `main.py`. The output will be in the `output` directory.
